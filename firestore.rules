rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas temporales MUY PERMISIVAS para debugging
    // TODO: Implementar reglas de seguridad apropiadas
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}